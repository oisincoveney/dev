# .semgrep.yml — generated by @oisincoveney/dev
# Run via: semgrep --config .semgrep.yml
rules: []
# We use community rulesets via the Semgrep registry:
# semgrep --config p/security-audit --config p/owasp-top-ten --config p/cwe-top-25
