# Required dev tools for this repo.
# Install everything with: `mise install` (https://mise.jdx.dev/).
#
# `bd` is required because src/install.ts shells out to it and the test suite
# runs against the real binary — there is no mock or fake path. If you don't
# want to use mise, install beads manually so `bd` is on PATH:
#   curl -sSL https://raw.githubusercontent.com/steveyegge/beads/main/scripts/install.sh | bash

[tools]
bun = "1.3"
"pipx:copier" = "9.14.0"
"npm:@sentry/dotagents" = "latest"
"aqua:evilmartians/lefthook" = "latest"
"aqua:steveyegge/beads" = "1.0.2"

[tasks.build]
run = "bun run build"

[tasks.dev]
run = "bun run test:watch"

[tasks.format]
run = "echo \"no format configured\""

[tasks.lint]
run = "echo \"no lint configured\""

[tasks.test]
run = "bun test"

[tasks.typecheck]
run = "tsc --noEmit"
