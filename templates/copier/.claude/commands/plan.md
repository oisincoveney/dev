---
description: Create tracker-backed work in review state and stop.
---

Follow the project `/plan` skill exactly.

Create tracker-backed work in review state and stop. Do not implement.

- Use Backlog.md directly with `backlog task create ... --draft`.
- Store workflow state in Backlog task fields: priority, dependencies, plan, notes, AC, DoD, refs, and final summary.
- Include priority rationale, scope/files, acceptance criteria, verification commands, and PR grouping when relevant.
- Single-task and multi-ticket plans both require review.

Goal: $ARGUMENTS
